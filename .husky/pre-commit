#!/usr/bin/env sh
set -e

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required for pre-commit checks."
  echo "Install it with: brew install gitleaks"
  exit 1
fi

npm run precommit:checks
